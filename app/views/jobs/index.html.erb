<h1>Listing jobs</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Event</th>
    <th>Done</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @jobs.each do |job| %>
  <tr>
    <td><%= link_to job.name, edit_job_path(job) %></td>
    <td><%= job.event_id %></td>
    <td><%= job.done %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Job', new_job_path %>
