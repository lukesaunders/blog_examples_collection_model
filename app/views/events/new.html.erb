<h1>New event</h1>

<%= form_for(@event) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <div class="actions">
    <%= f.submit 'Create event' %>
  </div>
<% end %>

<%= link_to 'Back', events_path %>
