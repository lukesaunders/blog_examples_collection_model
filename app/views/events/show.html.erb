<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @event.name %>
</p>


<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
<%= link_to('Create event jobs', new_event_job_collection_path(:event_id => @event.id)) unless @event.jobs_finalized? %>
